{/* <Modal
open={open} 
onClose={handleClose}
aria-labelledby="modal-modal-title"
>
<Box sx={{
  position: 'absolute',
  top: '50%',
  left: '50%',
  transform: 'translate(-50%, -50%)',
  width: 300,
  bgcolor: 'background.paper',
  border: '2px solid #000',
  boxShadow: 24,
  p: 4,
}}>
  <Typography id="modal-modal-title" variant="text" component="h5">
    Email = komal@amoga.co
  </Typography>
</Box>
</Modal> */}
// renderCell:(params)=> (
//     <Button onClick={handleOpen}>{params.row.parent_name}</Button>
//   )

// {(data && data.home) &&
//     <div>{data.home[0].title}</div>
// }    